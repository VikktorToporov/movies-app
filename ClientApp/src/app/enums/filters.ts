export enum Genres {
    Action = 0,
    Adventure = 1,
    Comedy = 2,
    Crime = 3,
    Drama = 4,
    Fantasy = 5,
}

export enum Show {
    all = 0,
    rated = 1,
    unrated = 2,
}

export enum SortColumn {
    ImdbRating = 0,
    YourRating = 1,
    Year = 2
}

export enum SortOrder {
  asc = 0,
  desc = 1
}